{
  "name": "pi-devin-oauth",
  "version": "0.1.5",
  "description": "Devin models in Pi without the Devin CLI.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "devin",
    "cognition",
    "custom-provider"
  ],
  "author": "ttttmr",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ttttmr/pi-devin-oauth.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "pi": {
    "extensions": [
      "./dist/extension.js"
    ]
  },
  "files": [
    "dist/",
    "extensions/",
    "src/",
    "README.md",
    "README_zh.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test src/chat-context-map.test.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build && tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "^24.13.3",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
